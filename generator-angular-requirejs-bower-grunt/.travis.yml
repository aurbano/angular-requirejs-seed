language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-requirejs-bower-grunt' ]; then cd .. && eval "mv $currentfolder generator-angular-requirejs-bower-grunt" && cd generator-angular-requirejs-bower-grunt; fi
